apply from: '../app.gradle'

android {
    namespace 'cn.jkdev.video'
    resourcePrefix 'video_'

    defaultConfig {
        if (!isRelease) {
            applicationId "cn.jkdev.video"
            versionCode 1
            versionName "1.0"
        }
    }
}

dependencies {}
